export interface Category {
    id: number;
    title: string; // done
    description: string; // done
    mode: string; // done
    cover: string; // done
    created_at?: string;
    updated_at?: string;
}